---
name: cost
description: Calculates and displays session token usage and estimated cost ($) for current AI agent session across AGY, Claude Code, OpenCode, and Codex harnesses.
metadata:
  opencode/slash: "true"
---

# Session Cost Calculator (`/cost`)

When invoked, execute the cost calculation script:

```bash
python3 scripts/cost.py
```
